import useFetch from "../hooks/useFetch"

const ProductCard = () => {

    const { apiData } = useFetch("");

    return (
        <>
            <section>

            </section>
        </>
    )
}

export default ProductCard